global:
  resolve_timeout: 5m

route:
  receiver: "slack"
  group_wait: 10s
  group_interval: 10s
  repeat_interval: 1h

receivers:
  - name: "slack"
    slack_configs:
      - api_url: "${ALERT_SLACK_WEBHOOK}"
        channel: "#defi-agent-alerts"
